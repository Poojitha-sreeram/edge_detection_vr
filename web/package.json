{
  "name": "edge-detection-viewer",
  "version": "1.0.0",
  "description": "TypeScript web viewer for real-time edge detection",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "http-server -p 8080"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
